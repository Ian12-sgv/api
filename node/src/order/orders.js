//manejo de rutas en express

const router = Router();

// POST /orders
router.post("/" /* handler */);

// GET /orders/:id
router.get("/:id" /* handler */);

// PATCH /orders/:id
router.patch("/:id" /* handler */);

// POST /orders/:id/cancel
router.post("/:id/cancel" /* handler */);

export default router;
